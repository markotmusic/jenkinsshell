pipeline {
    agent any
    parameters {
        string(name: 'TOPIC_NAME', defaultValue: 'DevOps', description: 'Topics to learn')
        string(name: 'TARGET', defaultValue: 'Dream-Job', description: 'Target goal')
    }

    stages {
        //stage('Clone Repo') {
        //    steps {
        //        checkout scm
        //    }
        //}
        stage('Call Script') {
            steps {
                sh 'chmod +x sayhello.sh'
                sh 'sayhello.sh test1 test2'    //ova komanda nije ispravna, exit code 127, komanda nije prepoznata!
            }
        }
    }
}
